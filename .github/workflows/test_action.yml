name: Test action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build test fap
      uses: oleksiikutuzov/flipperzero-ufbt-action@${{ github.ref_name }}
      with:
        fap-dir: test
        channel: rc
