name: Test action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build test fap
      uses: oleksiikutuzov/flipperzero-ufbt-action@${{ GITHUB_REF_NAME }}
      with:
        fap-dir: test
        channel: rc